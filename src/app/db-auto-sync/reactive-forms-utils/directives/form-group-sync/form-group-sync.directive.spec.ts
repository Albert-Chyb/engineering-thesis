import { FormGroupSyncDirective } from './form-group-sync.directive';

describe('FormGroupSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupSyncDirective();
    expect(directive).toBeTruthy();
  });
});
